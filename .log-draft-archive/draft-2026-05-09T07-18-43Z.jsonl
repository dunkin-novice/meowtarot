{"id":"2026-05-09-asset-config-capacitor-protocol","date":"2026-05-09","type":"fix","status":"shipped","goal":"Stop iOS Simulator from redownloading every card art image on every reload once the Capacitor wrap lands. Pre-emptive fix during Track 1.11 scaffold.","done":"Added a protocol === 'capacitor:' early return in isLocalDevHost() in js/asset-config.js. The function previously matched on hostname only ('localhost' / '127.0.0.1' / '[::1]'), which collides with capacitor://localhost — Capacitor's WKWebView scheme. The collision routed Capacitor through the dev-revision branch (dev-${Date.now()}), busting the immutable CDN cache on every reload. Guarding inside isLocalDevHost means any future caller of that helper also benefits, not just getDefaultAssetRevision.","root_cause":"isLocalDevHost identified Capacitor's WebView scheme as a dev environment because both share the 'localhost' hostname. Protocol was the discriminator we never checked.","fix":"Early-return false in isLocalDevHost when window.location.protocol === 'capacitor:'.","files":["js/asset-config.js"],"verified":"node --test passes 64/65 — the one failing test (tests/poster-debug-ci.test.js) is environmental (requires playwright in node_modules; pre-existing failure unrelated to this change). No other test references asset-config.","guardrails_preserved":["asset-resolver-untouched","off-limits-untouched","en-th-parity","i18n-via-common-only","free-core-complete","legacy-share-links-compat","no-direct-gtag","desktop-unchanged"],"notes":"Lands directly on main, no feature branch — surgical 4-line guard with non-controversial scope. Companion to Track 1.11 Capacitor scaffold (separate branch). Has zero runtime effect on the live site (production is https://, http://localhost is unaffected); only matters once a Capacitor wrap exists."}
