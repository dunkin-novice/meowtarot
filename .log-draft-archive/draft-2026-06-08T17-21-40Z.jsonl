{"id":"2026-06-09-bug016-3-deck-pin","date":"2026-06-09","type":"fix","status":"shipped","goal":"Stop the result page and share poster from showing different decks after an authed deck-unlock claim (BUG-016 #3)","done":"Authed 'Use this deck now' now writes meowtarot_pending_deck_claim instead of calling setActiveDeck() live, so the current reading + its poster stay on the deck they were drawn with; auth.js applies the pending deck on the next page load. js/deck-reward.js (CTA rewire + drop unused setActiveDeck import).","files":["js/deck-reward.js"],"symptom":"After a logged-in user tapped 'Use this deck now' on the deck-unlock popup, the result page kept the drawn deck's card art but a share poster generated afterwards rendered the newly-activated deck — mismatched decks.","root_cause":"Nothing pins the deck to a reading: js/reading.js renders the result once via getActiveDeckId() and share/poster.js resolves card art from the live active deck at generation time. The authed CTA's synchronous setActiveDeck() changed the live deck between those two reads, splitting result vs poster. The share payload has no deck field.","fix":"Replaced setActiveDeck(deck.id) in the authed CTA with localStorage.setItem('meowtarot_pending_deck_claim', deck.id) — the same deferral the unauth path uses. getActiveDeckId reads only meowtarot_active_deck, so the current page is unaffected; auth.js onAuthStateChange (L58-64) activates the pending deck on the next load.","verified":["node --check js/deck-reward.js","getActiveDeckId reads only ACTIVE_DECK_STORAGE_KEY (data.js:117-120)","chrome-devtools-mcp live: writing meowtarot_pending_deck_claim leaves meowtarot_active_deck unchanged (activeUnchanged:true)"],"affects":["daily reading result + share poster for logged-in users who claim a deck unlock"],"related":["BUG-016","BUG-019","BUG-020"],"guardrails_preserved":["asset-resolver-untouched","legacy-share-links-compat","free-core-complete","i18n-via-common-only"],"filed":["future feature: restyle the on-screen reading to the new deck on 'Use this deck now' tap (currently the switch applies from the next reading)"],"notes":"Could not exercise the full logged-in popup->reload->activate round trip (no test account); that path is code-traced. Mirror sync (www/ios) deferred — pre-existing drift (BUG-015); web serves canonical."}
