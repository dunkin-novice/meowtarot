{"id":"2026-06-16-meaning-page-deck-match","type":"bug","status":"shipped","goal":"The 'Open Live Full Meaning Page' link showed the card in the DEFAULT deck (moonmallow) instead of the deck the reading was drawn with","files":["js/data.js","js/reading.js"],"root_cause":"The meaning-page link points to the canonical pages on https://www.meowtarot.com (getCanonicalCardUrl / buildCardMeaningUrl), but the app + readings run on the apex meowtarot.com (and the iOS app on capacitor://localhost). buildCardImageUrls (asset-resolver) picks the deck via getActiveDeckId(), which reads the origin-scoped localStorage 'meowtarot_active_deck'. On the cross-origin www page that key is absent, so it fell back to DEFAULT_DECK_ID — mismatching the reading's deck. (Live /cards/<slug>/ pages are rendered by per-card js/pages/<card>-card.js scripts, all of which use getCardImageUrl -> buildCardImageUrls -> getActiveDeckId, so a single fix at getActiveDeckId covers all 78 + card-page.js.)","fix":"getActiveDeckId() now honors a ?deck= URL override (validated against DECKS) before reading localStorage — no persistence, no unlock gate, origin-independent. reading.js appends ?deck=<getActiveDeckId()> to the 'Open Live Full Meaning Page' button href (only that button; the SEO canonical stays clean). asset-resolver.js untouched (rule #4).","verified":"Local (chrome-devtools, cache-busted): /cards/death/?deck=boba-oracle -> hero image boba-oracle (ok); ?deck=not-a-real-deck -> moonmallow (guard); no param -> default. Reading button now carries the active deck across origins.","guardrails_preserved":["asset-resolver-untouched","en-th-parity","free-core-complete","legacy-share-links-compat","desktop-unchanged"]}
