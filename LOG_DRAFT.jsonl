{"id":"2026-05-31-fix-veila-tarot-first-login-grant","date":"2026-05-31","type":"fix","status":"shipped","goal":"Grant the veila-tarot deck on first Google sign-in. The login-reward popup was supposed to do this on its CTA, but the grant action did not exist anywhere in the codebase.","symptom":"Users completing their first Google OAuth sign-in saw the welcome popup, clicked the CTA, the popup dismissed — and nothing else happened. Veila Tarot did not become the active deck. The popup itself also advertised the wrong deck art (Moonmallow card back instead of Veila).","root_cause":"Two defects in js/login-reward.js: (1) the deckBackUrl computed at the popup-build site referenced DECKS.moonmallow.backImage, so the modal showed the starter deck art instead of the deck being granted; (2) the CTA click handler called only markSeen() + dismiss() — no setActiveDeck(), no grant call, nothing that would actually transfer veila-tarot to the user. Grepping js/ for grantDeck, unlockDeck, ownedDecks, or any veila reference confirmed there is no per-user ownership model in the code; the only mechanism that would land veila on the user is setActiveDeck('veila-tarot'), which was never invoked from the CTA. Detection chain via auth.js onAuthStateChange → maybeShowLoginReward was wired correctly; only the grant step inside the CTA was missing.","fix":"Three functional edits in js/login-reward.js: (a) added setActiveDeck to the existing import from ./data.js so the function is in scope; (b) changed the popup-build deckBackUrl reference from DECKS.moonmallow.backImage to DECKS['veila-tarot'].backImage so the modal shows Veila's card back; (c) added setActiveDeck('veila-tarot') as the first line of the CTA click handler, before markSeen() and dismiss(). Also updated the file-header docstring (line 5) to reference Veila Tarot instead of Moonmallow and to note that the CTA grants the deck. canUnlockDeck('veila-tarot') already returns true once a user is signed in (deck has role: 'default' in js/data.js line 24), so setActiveDeck succeeds without further changes.","files":["js/login-reward.js"],"affects":["First-time Google sign-in users on any page that triggers maybeShowLoginReward (called from auth.js onAuthStateChange) — popup now shows Veila Tarot card back and clicking the CTA makes Veila Tarot the active deck. Returning users (meowtarot_login_reward_seen=true) are unaffected. Per-device flag, so the popup re-fires once per device for users who clear localStorage."],"guardrails_preserved":["asset-resolver-untouched","en-th-parity","i18n-via-common-only","free-core-complete","legacy-share-links-compat","no-direct-gtag","desktop-unchanged"],"notes":"The per-device flag pattern (localStorage 'meowtarot_login_reward_seen') means this fix lands only on devices that have not yet seen the popup. Devices where the previous broken CTA was already clicked carry seen=true and will never see the corrected popup unless localStorage is cleared. If that matters, a follow-up could bump the storage key (e.g. meowtarot_login_reward_seen_v2) to re-fire for everyone. Not done in this commit because it would also re-fire for users who legitimately dismissed the old popup. Flag retained for product decision.","verified":["node --check js/login-reward.js: OK","grep import.*setActiveDeck js/login-reward.js: present at line 10","grep DECKS\\['veila-tarot'\\] js/login-reward.js: present at line 223","grep setActiveDeck.*veila-tarot js/login-reward.js: present at line 243 inside the CTA handler","md5 canonical == www == ios after cp + cap sync (27bfff894ee2a0e9a8f40e1c18eaaf56)"],"schema_version":1}
